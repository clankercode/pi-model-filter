# pi-model-filter

A pi coding agent extension that lets you filter, block, and modify models from any provider via a declarative config.

## Install

```bash
pi install ./path/to/pi-model-filter
```

## Configuration

Create `~/.pi/model-filter.json`. See plan for full schema and examples.

## Status

🚧 Under development
